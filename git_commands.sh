# this is ashell script but with work on windows too, 
# pure git commands

#to clone first time
git clone <https of the repo>

git clone https://github.com/gleon-magileap/vladihegu-gleon.git

# then you get into the folder you just clone

cd vladihegu-gleon

#to get latest changes
git pull

# to puh changes
git add .
git commit -m "any message"
git push 